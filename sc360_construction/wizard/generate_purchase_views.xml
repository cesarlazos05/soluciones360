<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ============================================================ -->
    <!-- Generate Purchase Wizard Form                                -->
    <!-- ============================================================ -->

    <record id="view_generate_purchase_wizard_form" model="ir.ui.view">
        <field name="name">sc360.generate.purchase.wizard.form</field>
        <field name="model">sc360.generate.purchase.wizard</field>
        <field name="arch" type="xml">
            <form string="Generar ordenes de compra">
                <group>
                    <field name="requisition_id" readonly="1"/>
                </group>
                <separator string="Lineas a comprar"/>
                <p class="text-muted">
                    Asigne un proveedor y precio a cada linea. Se generara una ODC por cada proveedor diferente.
                </p>
                <field name="line_ids">
                    <list editable="bottom">
                        <field name="requisition_line_id" column_invisible="1"/>
                        <field name="product_id" readonly="1"/>
                        <field name="description" readonly="1"/>
                        <field name="quantity"/>
                        <field name="uom_id" readonly="1"/>
                        <field name="partner_id"/>
                        <field name="price_unit"/>
                    </list>
                </field>
                <footer>
                    <button name="action_generate" type="object"
                            string="Generar ODCs" class="btn-primary"/>
                    <button string="Cancelar" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

</odoo>
